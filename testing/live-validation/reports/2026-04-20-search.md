# Live Validation Report: Search

- **Date:** 2026-04-20
- **Checklist:** testing/live-validation/checklists/06-search.md
- **Operator:** Claude Code

## Summary

- Items run: 15
- Passed: 14
- Failed: 1

## Failures

### 3.4 Negative context

- **Checklist item:** testing/live-validation/checklists/06-search.md §3.4
- **Action run:** `search` — query `unique_sentinel_mcp_test_xyz`, type `simple`, contextLength `-1`
- **Expected:** either error (missing validation) or success behaving as if `0` or some default
- **Observed:** success, but `matches[0].context` is `"nique_sentinel_mcp_test_xy"` — the negative value is applied as an offset that trims 1 character from each side of the match token rather than being rejected or treated as 0/default
- **Category:** missing validation

## Passes

All other items in the checklist passed.

Notes on observed behavior (non-failing):

- §2.1: result shape is `{filename, score, matches[]}`; each match element has `match: {start, end, source}` and `context` (no `offset` top-level key). Results sorted most-negative `score` first.
- §3.3: contextLength `0` yields `context` equal to the match token itself (e.g., `"unique_sentinel_mcp_test_xyz"`), not empty string.
- §4.1: Dataview is installed/enabled; TABLE query succeeded, so proceeded to 4.2.
- §5.1: empty query returns `[]` (success, not error).
- §5.2: 20× repeated token query succeeded and returned a match with positive `score` (5.3833).
- §5.3: `[test](match)` returns `[]` with no regex-compilation error.

## Setup/teardown

- Setup: passed — `vault_create` of `sandbox/search.md` succeeded; 1.2s wait for index.
- Teardown: passed — `vault_delete` of `sandbox/search.md` succeeded.
