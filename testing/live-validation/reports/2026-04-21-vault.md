# Live Validation Report: Vault Lifecycle

- **Date:** 2026-04-21
- **Checklist:** testing/live-validation/checklists/01-vault.md
- **Operator:** Claude Code

## Summary

- Items run: 17
- Passed: 16
- Failed: 1

## Failures

### 4.4 List a file path

- **Checklist item:** testing/live-validation/checklists/01-vault.md §4.4
- **Action run:** `vault_list` — path `sandbox/dir_test.md`
- **Expected:** error indicating the path is not a directory
- **Observed:** error `Directory not found: sandbox/dir_test.md` — message implies the directory is absent, but the path exists as a file; caller can't distinguish "not a directory" from "doesn't exist"
- **Category:** error shape

_Note: the 2026-04-20 report passed this item under the checklist's "(or equivalent)" clause. The underlying behavior is unchanged; this session reclassified the observation as a failure because the error shape is genuinely ambiguous for callers._

## Passes

All other items in the checklist passed.

Notes on observed behavior (not failures):

- §5.1 (`sandbox/no_ext`), §6.1 (`sandbox/data.json`), §6.2 (`sandbox/plain.txt`): files created and readable via the API, but not visible in Obsidian's default file explorer UI. Obsidian hides files without recognized extensions (and by default hides `.txt`/`.json`) from the file tree. Tool behavior is correct; operator should be aware that created files may be invisible in the UI.
- §5.3 (case sensitivity): filesystem behaved case-sensitively — `vault_read` at `sandbox/uppercase.md` returned `File not found` after creating `sandbox/UPPERCASE.md`.

## Setup/teardown

- Setup: passed — no setup actions required; `sandbox/` creatable.
- Teardown: passed — all 6 remaining sandbox files deleted cleanly.
