# Live Validation Report: Search

- **Date:** 2026-04-21
- **Checklist:** testing/live-validation/checklists/06-search.md
- **Operator:** Claude Code

## Summary

- Items run: 15
- Passed: 14
- Failed: 1

## Failures

### 3.4 Negative context

- **Checklist item:** testing/live-validation/checklists/06-search.md §3.4
- **Action run:** `search` — query `unique_sentinel_mcp_test_xyz`, type `simple`, contextLength `-1`
- **Expected:** error (ideal: `missing validation`), or success behaving as contextLength `0` / default
- **Observed:** success; `context` returned as `"nique_sentinel_mcp_test_xy"` — the negative value is applied as an offset that trims 1 char from each side of the matched token rather than being rejected or defaulting
- **Category:** missing validation

## Passes

All other items in the checklist passed.

## Observations

- **2.1 result size:** searching the common word `search` across the full vault produced a response of ~218 KB / ~5,400 lines, which exceeded the harness per-tool-result token cap and was spilled to a tmp file. The tool itself worked correctly, but broad queries against a real vault can produce payloads large enough to be impractical for an MCP caller. Worth considering a result limit, pagination, or a filename-only mode for the `search` tool.
- **Score sign inconsistency:** 1.1, 2.1, 3.1–3.4 returned negative scores (best match = most negative). 5.2 returned a *positive* score (5.38) on the same `sandbox/search.md` match. The sort-by-most-negative convention documented in 2.1 does not always hold — score polarity appears to depend on query characteristics.
- **3.3 exact behavior:** `contextLength: 0` returns `context` containing only the match token itself (no surrounding characters).
- **4.1 branch:** Dataview plugin was present in this vault, so the "Dataview absent" path was not exercised; proceeded to 4.2.
- **Match object shape:** each entry's `matches[]` element contains `context` (string) and `match` (object with `start`, `end`, `source`).

## Setup/teardown

- Setup: passed
- Teardown: passed
