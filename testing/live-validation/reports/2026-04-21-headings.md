# Live Validation Report: Heading Targeting

- **Date:** 2026-04-21
- **Checklist:** testing/live-validation/checklists/02-headings.md
- **Operator:** Claude Code

## Summary

- Items run: 22
- Passed: 21
- Failed: 1

## Failures

### 5.2 No leading newline merges with prior content

- **Checklist item:** testing/live-validation/checklists/02-headings.md §5.2
- **Action run:** `vault_update` — path `sandbox/headings.md`, targetType `heading`, target `Epsilon`, operation `append`, content `No leading newline.` (no leading newline)
- **Expected:** appended text adjacent to prior Epsilon body content (no blank-line separator inserted)
- **Observed:** tool inserted a blank line between the trailing Epsilon body text (`Zeta content.`) and `No leading newline.`, producing blank-line separation rather than direct adjacency
- **Category:** behavior mismatch

## Passes

All other items in the checklist passed.

## Notes / observations

The auto-separator behavior flagged in 5.2 also surfaced in passing items where the append payload included a leading `\n`:

- **6.2** — `vault_update` append `\nRegression probe.` to `Epsilon`. Expected one blank line between `No leading newline.` and `Regression probe.` (one trailing `\n` + one leading `\n`); observed two blank lines.
- **7.1** — `vault_update` append `\nSentinel text.` to `Epsilon::Zeta`. Expected one blank line between `Regression probe.` and `Sentinel text.`; observed two blank lines.

Both items pass their checklist assertions (success / heading still targetable), but the spacing anomaly is consistent with 5.2: `vault_update` heading append appears to inject an extra `\n` before the payload regardless of whether the caller already supplied leading whitespace.

## Setup/teardown

- Setup: passed
- Teardown: passed — all four sandbox files deleted cleanly
