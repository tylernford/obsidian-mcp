# Live Validation Report: Block Reference Targeting

- **Date:** 2026-04-21
- **Checklist:** testing/live-validation/checklists/03-blocks.md
- **Operator:** Claude Code

## Summary

- Items run: 9
- Passed: 7
- Failed: 2

## Failures

### 1.1 Append to block

- **Checklist item:** testing/live-validation/checklists/03-blocks.md §1.1
- **Action run:** `vault_update` — path `sandbox/blocks.md`, targetType `block`, target `block1`, operation `append`, content `\nAppended after block1.`
- **Expected:** `Appended after block1.` immediately following the `First paragraph. ^block1` paragraph
- **Observed:** append succeeded and landed after `First paragraph.`, but the `^block1` marker was relocated from `First paragraph.` to the appended line (`Appended after block1. ^block1`) — marker tracks the last line of the block, which is unexpected for callers assuming the anchor stays on the original line
- **Category:** behavior mismatch

### 3.1 Create a new block

- **Checklist item:** testing/live-validation/checklists/03-blocks.md §3.1
- **Action run:** `vault_update` — target `newblock`, operation `append`, content `New block body.`, `createIfMissing: true`
- **Expected:** a line containing `New block body.` with `^newblock` attached (conventional inline form)
- **Observed:** block created at file end with `^newblock` on its own line separated by a blank line from `New block body.` rather than inline (`New block body. ^newblock`); id still resolves but layout is unexpected
- **Category:** behavior mismatch

## Passes

All other items in the checklist passed (1.2, 1.3, 2.1, 2.2, 3.2, 4.1, 4.2).

## Setup/teardown

- Setup: passed
- Teardown: passed
